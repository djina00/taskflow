<Window x:Class="TaskFlow.Desktop.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:TaskFlow.Desktop.Controls"
        mc:Ignorable="d"
        Title="TaskFlow — Sign in" Height="480" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize"
        Background="{StaticResource BgBrush}">
    <Grid>
        <ctrl:AuthCard Subtitle="Sign in to your account"
                       PrimaryText="Log in" PrimaryCommand="{Binding LoginCommand}"
                       LinkPrompt="Don't have an account?" LinkText="Create one"
                       LinkCommand="{Binding GoToRegisterCommand}"
                       StatusText="{Binding Status}">
            <StackPanel>
                <ctrl:LabeledField Label="Email">
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
                </ctrl:LabeledField>
                <ctrl:LabeledField Label="Password">
                    <PasswordBox x:Name="PasswordInput" />
                </ctrl:LabeledField>
            </StackPanel>
        </ctrl:AuthCard>
    </Grid>
</Window>
