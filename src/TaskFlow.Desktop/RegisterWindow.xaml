<Window x:Class="TaskFlow.Desktop.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:TaskFlow.Desktop.Controls"
        mc:Ignorable="d"
        Title="TaskFlow — Create account" Height="560" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize"
        Background="{StaticResource BgBrush}">
    <Grid>
        <ctrl:AuthCard Subtitle="Create your account"
                       PrimaryText="Create account" PrimaryCommand="{Binding RegisterCommand}"
                       LinkPrompt="Already have an account?" LinkText="Sign in"
                       LinkCommand="{Binding GoToLoginCommand}"
                       StatusText="{Binding Status}">
            <StackPanel>
                <ctrl:LabeledField Label="Full name">
                    <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" />
                </ctrl:LabeledField>
                <ctrl:LabeledField Label="Email">
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
                </ctrl:LabeledField>
                <ctrl:LabeledField Label="Password">
                    <PasswordBox x:Name="PasswordInput" />
                </ctrl:LabeledField>
            </StackPanel>
        </ctrl:AuthCard>
    </Grid>
</Window>
